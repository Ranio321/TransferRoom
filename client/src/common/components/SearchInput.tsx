import { TextField } from "@mui/material";

export const SearchInput = () => {
  return (
    <TextField
      sx={{ paddingBottom: 5, margin: "auto" }}
      label="Search Clubs"
      id="fullWidth"
    />
  );
};
